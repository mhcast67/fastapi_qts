import time

def processar_pagamento(valor: float,) -> bool:
    """Simula o tempo de um gateway de Pagamentos"""
    if valor <- 0:
        return False

    # Simula uma latencia de rede ou comunicação com API externa
    time.sleep(0.05)
    return True
